import { WrapperStub } from "@/styles/globals";
import styled from "styled-components";

export const AddTagsContainer = styled.div`
  position: relative;
`;
export const Wrapper = styled(WrapperStub)`
  margin-top: 46px;
`;

export const Container = styled(WrapperStub)`
  padding: 46px 68px;
`;
